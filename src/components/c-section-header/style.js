import styled from "styled-components";

const SeactionHeaderWrapper=styled.div`
  margin-bottom: 20px;
  color:${props=>props.theme.font.primaryColor};
  .sub-title {
    font-size: 16px;
  }
`

export default SeactionHeaderWrapper;